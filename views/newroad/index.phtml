
<?php
$url_order = 'ASC';
if ($order_by == 'road_id')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';

elseif ($order_by == 'road_from')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'road_to')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'road_km')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'road_oil')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'road_time')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'bridge_cost')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'police_cost')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'tire_cost')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'way')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'charge_add')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'start_time')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'end_time')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';
elseif ($order_by == 'status')
    $url_order = $order == 'ASC' ? 'DESC' : 'ASC';


    $i = $sonews*$page-($sonews-1);

?>

<div id="loading"></div>
<div id="winpopup"></div>
<center style="clear:both;margin-bottom: -30px;"><h1> THÊM KHUNG ĐỊNH MỨC </h1></center>
<div id="content" style="padding:5px;">
    
<div class="add-box">
    <a class="add_button" onClick="add_click();">Thêm bảng định mức</a>
    <a class="add_button" id="import_excel" href="<?= $this->url('road/import')?>">Import Excel</a>
    <a class="add_button" id="import_excel" href="<?= $this->url('road/export')?>">Export Excel</a>
</div>
	<div class="search-box">
        
        <input type="search" id="search-input" name="s" value="<?php echo isset($keyword)?$keyword:null; ?>" placeholder="Tìm kiếm">
        <input type="button" name="" id="search-submit" class="button-search" value="Tìm kiếm" onclick="searchall('<?php echo $page ?>','<?php echo $order_by ?>','<?php echo $order ?>');">
    </div>
    <div class="tablenav top">
        <div class="add-box">
            <input type="button" name="chon" id="chon" value="Chọn" onclick="searchall('<?php echo $page ?>','<?php echo $order_by ?>','<?php echo $order ?>');">  
        </div>
        <div class="add-box" style="margin-right: 5px">
            

            <select name="sl_round" id="sl_round" >
                <option selected="selected" value="1">Đang sử dụng</option>
                <option value="0">Không sử dụng</option>
            </select>


            
        </div>
    	<div class="alignleft actions">
			<select name="action" id="action">
            	<option value="-1" selected="selected">Chọn</option>
                
                <option value="delete">Xóa</option>
            </select>
            <input type="button" name="" id="doaction" class="button action" value="Áp dụng" onclick="action();">
		</div>
		<div class="alignleft actions">
		<select name="m" id="chonloc">
			<option  value="18446744073709">Hiển thị tất cả</option>
			<option value="5">Hiển thị 5 giá trị</option>
            <option value="10">Hiển thị 10 giá trị</option>
            <option value="15">Hiển thị 15 giá trị</option>
            <option selected="selected" value="20">Hiển thị 20 giá trị</option>
		</select>
		<input type="button" name="" id="post-query-submit" class="button" value="Chọn lọc" onclick="searchall('<?php echo $page ?>','<?php echo $order_by ?>','<?php echo $order ?>');">						 		
        </div>

      </div>

      <div style="clear:both"></div>
      <div class="tablenav top">
          <div class="add-box">
                <input type="button" name="chon" id="chon" value="Chọn" onclick="searchall('<?php echo $page ?>','<?php echo $order_by ?>','<?php echo $order ?>');">  
            </div>

            <div class="add-box" style="margin-right: 5px">
                <select style="width:120px" name="ketthuc" id="ketthuc" >
                    <option selected="selected" value="0">Kho đến</option>
                    <?php foreach ($warehouses as $ware) { ?>
                        <option value="<?php echo $ware->warehouse_id ?>"><?php echo $ware->warehouse_name ?></option>
                    <?php } ?>
                    
                </select>

           </div>

            <div class="add-box" style="margin-right: 5px">
                 <select style="width:120px" name="batdau" id="batdau" >
                    <option selected="selected" value="0">Kho đi</option>
                    <?php foreach ($warehouses as $ware) { ?>
                        <option value="<?php echo $ware->warehouse_id ?>"><?php echo $ware->warehouse_name ?></option>
                    <?php } ?>
                </select>
            </div>
        


            
        </div>

</div>

<table class="table_data">
<thead>
    <tr>
    	<th  class="fix"><input type="checkbox" onclick="checkall('checkbox', this)" name="checkall"/></th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','road_id','<?php echo $url_order ?>')">STT <?php if ($order_by == 'road_id'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','road_from','<?php echo $url_order ?>')">Kho đi <?php if ($order_by == 'road_from'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','road_to','<?php echo $url_order ?>')">Kho đến <?php if ($order_by == 'road_to'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','way','<?php echo $url_order ?>')">Chiều đi <?php if ($order_by == 'way'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','road_km','<?php echo $url_order ?>')">Khoảng cách (km)<?php if ($order_by == 'road_km'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','road_oil','<?php echo $url_order ?>')">Định mức lít dầu <?php if ($order_by == 'road_oil'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','bridge_cost','<?php echo $url_order ?>')">Cầu đường <?php if ($order_by == 'bridge_cost'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','police_cost','<?php echo $url_order ?>')">Công an <?php if ($order_by == 'police_cost'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','tire_cost','<?php echo $url_order ?>')">Vá vỏ-Rửa xe<?php if ($order_by == 'tire_cost'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','charge_add','<?php echo $url_order ?>')">Cước vượt tải<?php if ($order_by == 'charge_add'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','road_time','<?php echo $url_order ?>')">Thời gian chạy (ngày)<?php if ($order_by == 'road_time'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','start_time','<?php echo $url_order ?>')">Áp dụng<?php if ($order_by == 'start_time'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','end_time','<?php echo $url_order ?>')">Hết hiệu lực<?php if ($order_by == 'end_time'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        <th  class="fix">
            <a class="sort <?php echo $order ?>" onclick="sapxep('<?php echo $page ?>','status','<?php echo $url_order ?>')">Trạng thái<?php if ($order_by == 'status'): ?><i class="icon-chevron-<?php echo $url_order == 'ASC' ? 'down' : 'up' ?>"></i><?php endif; ?></a>
        </th>
        
           
        <th  class="fix">&nbsp;</th>
    </tr>
    
   </thead>
   <tbody>
    <?php foreach ($roads as $road) : ?>
        <tr onClick="HighLightTR(this,'#4d90fe','cc3333');" id="<?php echo $road->road_id ?>" class="edit_tr">
        	<td><input name="check[]" type="checkbox" class="checkbox" value="<?php echo $road->road_id ?>"></td>
            <td class="fix"><?php echo $i++; ?></td>
            
            <td nowrap class="fix" data="<?php echo $road->road_from; ?>" id="road_from_<?php echo $road->road_id; ?>"><?php echo $road->road_from==$warehouse['warehouse_id'][$road->road_from]?$warehouse['warehouse_name'][$road->road_from]:null ?></td>
            <td nowrap class="fix" data="<?php echo $road->road_to; ?>" id="road_to_<?php echo $road->road_id; ?>"><?php echo $road->road_to==$warehouse['warehouse_id'][$road->road_to]?$warehouse['warehouse_name'][$road->road_to]:null ?></td>
            
            <td class="fix" data="<?php echo $road->way; ?>" id="way_<?php echo $road->road_id; ?>"><?php echo $road->way==1?"Lên":($road->way==2?"Xuống":($road->way==3?"Bằng":($road->way==4?"ĐN-QN":"Rỗng"))); ?></td>
            <td class="fix" id="road_km_<?php echo $road->road_id; ?>"><?php echo $road->road_km; ?></td>
            <td class="fix" id="road_oil_<?php echo $road->road_id; ?>"><?php echo $road->road_oil; ?></td>
            <td class="fix" id="bridge_cost_<?php echo $road->road_id; ?>"><?php echo $lib->formatMoney(round($road->bridge_cost*1.1)); ?></td>
            <td class="fix" id="police_cost_<?php echo $road->road_id; ?>"><?php echo $lib->formatMoney($road->police_cost); ?></td>
            <td class="fix" id="tire_cost_<?php echo $road->road_id; ?>"><?php echo $lib->formatMoney($road->tire_cost); ?></td>
            <td class="fix" id="charge_add_<?php echo $road->road_id; ?>"><?php echo $lib->formatMoney($road->charge_add); ?></td>
            <td class="fix" id="road_time_<?php echo $road->road_id; ?>"><?php echo $road->road_time; ?></td>
            <td class="fix" id="start_time_<?php echo $road->road_id; ?>"><?php echo $lib->hien_thi_ngay_thang($road->start_time); ?></td>
            <td class="fix" id="end_time_<?php echo $road->road_id; ?>"><?php echo $lib->hien_thi_ngay_thang($road->end_time); ?></td>
            <td class="fix" data="<?php echo $road->status; ?>" id="status_<?php echo $road->road_id; ?>"><?php echo $road->status==1?'Sử dụng':'Không sử dụng'; ?></td>
            
            <td>
                <a class="error" href="#">Sửa</a>
                | 
                <a class="error" onclick="del(<?php echo $road->road_id ?>)" >Xóa</a>
            </td>
        </tr>
    <?php endforeach; ?>
   </tbody>
</table>
<?php
$this->helper('slidePaginator');
?>

<div id="error" class="error"><?php echo isset($error) ? $error : null; ?></div>

<div class="add-field">
        <fieldset class="groupbox">
            <legend>
                <span><h3>Bảng định mức</h3></span>
            </legend>
            <div class="login_body">
                  <form id="add_road" method="post" action="">
                      <table style="margin-top:0px;width:100%">
                        <tr>
                            
                            <td>&nbsp;</td>
                            <td>&nbsp;</td>
                            <td>&nbsp;</td>
                            <td>&nbsp;</td>
                            <td></td>
                            <td>&nbsp;</td>
                            <td><div class="add-box"><a class="add_button" onClick="add_click();">Thêm bảng định mức</a></div></td>
                            
                        </tr>
                        
                        <tr>
                            <td>Trạng thái </td>
                            <td>
                                <input type="radio" checked  name="status" tabindex="3" value="1"> Sử dụng
                                <input type="radio"  name="status"  value="0"> Không sử dụng
                            </td>
                            
                        </tr>
                        <tr>
                            
                            <td>Kho đi </td>
                            <td>
                                <input type="text" autofocus autocomplete="off"  id="road_from" name="road_from" tabindex="1" required="required" placeholder="Nhập tên hoặc * để chọn" >
                                <ul id="road_from_list_id"></ul>
                            </td>
                            <td>Kho đến </td>
                            <td>
                                <input type="text" autofocus autocomplete="off"  id="road_to" name="road_to" tabindex="2" required="required" placeholder="Nhập tên hoặc * để chọn" >
                                <ul id="road_to_list_id"></ul>
                            </td>
                            
                            <td>Ngày áp dụng</td>
                            <td><input class="ngay" type="text"   id="start_time" name="start_time" tabindex="10" required="required" ></td>
                            
                            
                        </tr>
                        
                        <tr>
                            
                            
                            
                            <td>Thời gian chạy (ngày)</td>
                            <td><input type="text" class="number" id="road_time" name="road_time" tabindex="6" required="required" ></td>
                            <td>Công an </td>
                            <td><input type="text" class="number" id="police_cost" name="police_cost" tabindex="8" ></td>
                            
                            <td>Ngày hết hạn</td>
                            <td><input class="ngay" type="text"   id="end_time" name="end_time" tabindex="11" required="required" ></td>
                        </tr>
                        <tr>
                            
                            
                            <td>Cầu đường </td>
                            <td><input disabled type="text" class="number" id="bridge_cost" name="bridge_cost" tabindex="7" ></td>
                            <td>Vá vỏ - Rửa xe </td>
                            <td><input type="text" class="number" id="tire_cost" name="tire_cost" tabindex="9" ></td>
                            <td>Cước vượt tải </td>
                            <td><input type="text" class="number" id="charge_add" name="charge_add" tabindex="12" ></td>
                        </tr>
                        
                        
                        <tr>
                            <td>Khoảng cách (km)</td>
                            <td>
                                <input disabled type="text" class="number"  id="road_km" name="road_km" tabindex="4" required="required">
                            </td>
                            <td>Định mức lít dầu </td>
                            <td><input type="text" disabled id="road_oil" class="number" name="road_oil" tabindex="5" required="required" ></td>
                            <td>Chiều đi </td>
                            <td colspan="2">
                                <input type="radio" checked  name="way" tabindex="3" value="0"> Rỗng
                                <input type="radio"  name="way"  value="1"> Lên
                                <input type="radio"  name="way"  value="2"> Xuống
                                <input type="radio"  name="way"  value="3"> Bằng
                                <input type="radio"  name="way"  value="4"> ĐN-QN
                            </td>
                        </tr>
                        <tr>
                            
                            <td colspan="3">
                                <table id="dataTable2" border="1" style="width: 100%; border: 1px solid rgb(221, 217, 217); margin-bottom: 10px" >
                                    <tbody>
                                        <tr>
                                            <td><input type="checkbox" name="chk2"></td>
                                            <td>
                                                <table style="width:100%">
                                                    <tr>
                                                        <td>Khoảng cách</td>
                                                        <td>
                                                            <input style="width:90px" type="text" class="distance_km number" name="distance_km[]" >
                                                        </td>
                                                        <td>Định mức lít dầu </td>
                                                        <td>
                                                            <input style="width:90px" type="text" disabled class="distance_oil" name="distance_oil[]" >
                                                        </td>
                                                        
                                                    </tr>
                                                    <tr>
                                                        <td>Chiều đi</td>
                                                        <td colspan="2">
                                                            <select style="width:90px" name="distance_way[]" class="distance_way" >
                                                                <option value="0">Rỗng</option>
                                                                <option value="1">Lên</option>
                                                                <option value="2">Xuống</option>
                                                                <option value="3">Bằng</option>
                                                                <option value="4">ĐN-QN</option>
                                                            </select>
                                                        </td>
                                                        
                                                    </tr>
                                                </table>
                                            </td>
                                        </tr>
                                        
                                    </tbody>
                                </table>
                                <input type="button" value="Thêm" onclick="addRow2('dataTable2')">
                                <input type="button" value="Xóa" onclick="deleteRow2('dataTable2')">
                            </td>
                        </tr>
                        <tr>
                            <td colspan="4">
                                <table id="dataTable" border="1" style="width: 100%; border: 1px solid rgb(221, 217, 217); margin-bottom: 10px" >
                                    <tbody>
                                        <tr>
                                            <td><input type="checkbox" name="chk"></td>
                                            <td>
                                                <table style="width:100%">
                                                    <tr>
                                                        <td>Trạm thu phí</td>
                                                        <td>
                                                            <input type="text" autocomplete="off" class="toll_booth_name" name="toll_booth_name[]" placeholder="Nhập tên hoặc * để chọn" >
                                                            <ul class="name_list_id"></ul>
                                                        </td>
                                                        <td>Giá vé</td>
                                                        <td><input type="text" class="toll_booth_cost number" name="toll_booth_cost[]">
                                                            <input type="checkbox" class="check_vat" name="check_vat[]" value="1"> VAT
                                                        </td>
                                                    </tr>
                                                    <tr>
                                                        <td>MST</td>
                                                        <td><input type="text" class="toll_mst" name="toll_mst[]"></td>
                                                        <td>Mẫu số</td>
                                                        <td><input type="text" class="toll_symbol" name="toll_symbol[]"></td>
                                                        <td>Loại</td>
                                                        <td>
                                                            <select name="toll_type[]" class="toll_type">
                                                                <option value="1">Vé thu phí</option>
                                                                <option value="2">Cước đường bộ</option>
                                                            </select>

                                                        </td>
                                                    </tr>
                                                    </tr>
                                                </table>
                                            </td>
                                        </tr>
                                        
                                    </tbody>
                                </table>
                                <input type="button" value="Thêm" onclick="addRow('dataTable')">
                                <input type="button" value="Xóa" onclick="deleteRow('dataTable')">
                            </td>
                        </tr>
                        
                        
                        
                        
                        <tr>
                            <td>&nbsp;</td>
                            <td><input type="hidden" readonly id="yes" name="yes" required="required" ></td>
                            <td>&nbsp;</td>
                        </tr>
                        
                        
                         <tr>
                            <td></td>
                            <td></td>
                            <td><input type="submit" name="submit" value="Hoàn tất" tabindex="13"></td>
                            <td ><input type="reset" name="reset" value="Nhập lại" tabindex="14"></td>
                            <td></td>
                            <td></td>
                             
                        </tr>
                    </table> 
                </form>
            </div>
        </fieldset>
    </div>
<script type="text/javascript">
$('html').click(function(e) {
    if(e.target == '[object HTMLDivElement]' || e.target == '[object HTMLBodyElement]') {
        $('.add-field').slideUp(500);    
    }
   $('#road_from_list_id').slideUp(200);
    $('#road_to_list_id').slideUp(200);
    $('.name_list_id').slideUp(200);
});
    $('.edit_tr').click(function(){
        if(!$('.checkbox').is(':checked')){
            $("html, body").animate({ scrollTop: $('.add-field').offset().top }, 300);
        }

        $('#dataTable tbody tr').remove();
        
        var road_id = $(this).attr('id');
        $.ajax({
            url: '<?php echo BASE_URL ?>/road/bridgecost',
            type: 'POST',
            data: {road:road_id},
            success:function(data){
                //alert(data);
                $('#dataTable tbody').append(data);
                $('.toll_booth_cost').keyup(function(){
                    var sum = 0;
                    $(".toll_booth_cost").each(function(){
                        sum += +$(this).val().replace(/[^0-9\.-]+/g,"");
                    });
                    $("#bridge_cost").val(sum);
                    $("#bridge_cost").val(function(index, value) {
                        return value
                          .replace(/[^0-9-.]/g, "")
                          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
                        ;
                      });
                });

                $('.toll_booth_name').keyup(function(){
                        var row = $(this).parent().parent().parent().parent().parent().parent();
                        var rowIndex = row[0].rowIndex;

                        var keyword = $(this).val();
                        $.ajax({
                            url: '<?php echo BASE_URL ?>/road/gettollbooth',
                            type: 'POST',
                            data: {keyword:keyword, offset:rowIndex},
                            success:function(data){
                                $('.name_list_id:eq('+rowIndex+')').slideDown(200);
                                $('.name_list_id:eq('+rowIndex+')').html(data);
                            }
                        });
                        if ($('.toll_booth_name:eq('+rowIndex+')').val() == "" || $('.toll_booth_name:eq('+rowIndex+')').attr('data') == "") {
                            
                            $('.toll_booth_name:eq('+rowIndex+')').attr('data',"");
                        }

                        
                    });

                $('.toll_booth_name').on('keydown', function() {
                        var row = $(this).parent().parent().parent().parent().parent().parent();
                        var rowIndex = row[0].rowIndex;

                        var key = event.keyCode || event.charCode;

                        if( key == 8 || key == 46 ){
                            $('.toll_booth_name:eq('+rowIndex+')').attr('data',"");
                        }
                            
                      });  

                function set_item_other(item,value,mst,type,symbol,vitri) {
                    // change input value
                    $('.toll_booth_name:eq('+vitri+')').val(value);
                    $('.toll_booth_name:eq('+vitri+')').attr("data",item);
                    $('.toll_mst:eq('+vitri+')').val(mst);
                    $('.toll_symbol:eq('+vitri+')').val(symbol);
                    $('.toll_symbol:eq('+vitri+') option[value='+type+']').attr('selected','selected');
                    // hide proposition list
                    $('.name_list_id').hide();
                    $('.toll_booth_name:eq('+vitri+')').focus();

                }

                function get_number(id){
                    return $(id).val().replace(/[^0-9\.-]+/g,"");
                }

                    $('.number').focus(function(){
                            if ( $(this).val() == '0') {
                               $(this).val(""); 
                            };
                            
                        });
                        $('.number').blur(function(){
                            if ( $(this).val() == "") {
                                $(this).val(0);
                            };
                            
                        });

                         $('.number').keyup(function(event) {

                          // skip for arrow keys
                      if(event.which >= 37 && event.which <= 40) return;

                      // format number
                      $(this).val(function(index, value) {
                        return value
                          .replace(/[^0-9-.]/g, "")
                          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
                        ;
                      });
                    });
            }
        });

        $('#dataTable2 tbody tr').remove();
        
        var road_id = $(this).attr('id');
        $.ajax({
            url: '<?php echo BASE_URL ?>/road/distance',
            type: 'POST',
            data: {road:road_id},
            success:function(data){
                //alert(data);
                $('#dataTable2 tbody').append(data);
                $('.distance_way').change(function() {
                var row = $(this).parent().parent().parent().parent().parent().parent();
                var rowIndex = row[0].rowIndex;

                var oil = 0;
                    if (this.value == 0) {
                                    
                        oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 38/100;
                        $('.distance_oil:eq('+rowIndex+')').val(oil);
                    }
                    else{
                        if (this.value == 1) {
                            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 58/100;
                            $('.distance_oil:eq('+rowIndex+')').val(oil);
                        }
                        else if (this.value == 2) {
                            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 54/100;
                            $('.distance_oil:eq('+rowIndex+')').val(oil);
                        }
                        else if (this.value == 3) {
                            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 45/100;
                            $('.distance_oil:eq('+rowIndex+')').val(oil);
                        }
                        else if (this.value == 4) {
                            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 48/100;
                            $('.distance_oil:eq('+rowIndex+')').val(oil);
                        }
                    }

                    var sum = 0;
                    $('.distance_km').each(function(){
                        sum += parseFloat(get_number(this));
                    });

                    $('#road_km').val(sum);
                    $("#road_km").val(function(index, value) {
                        return value
                          .replace(/[^0-9-.]/g, "")
                          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
                        ;
                      });

                    var sum_oil = 0;
                    $('.distance_oil').each(function(){
                        sum_oil += parseFloat(get_number(this));
                    });

                    $('#road_oil').val(sum_oil);
                    $("#road_oil").val(function(index, value) {
                        return value
                          .replace(/[^0-9-.]/g, "")
                          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
                        ;
                      });

                });

               $('.distance_km').keyup(function(){
                    var row = $(this).parent().parent().parent().parent().parent().parent();
                    var rowIndex = row[0].rowIndex;

                    var way = $('.distance_way:eq('+rowIndex+')').val();
                    var oil = 0;
                    if (way == 0) {
                        oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 38/100;
                        $('.distance_oil:eq('+rowIndex+')').val(oil);
                    }
                    else if (way == 1) {
                        oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 58/100;
                        $('.distance_oil:eq('+rowIndex+')').val(oil);
                    }
                    else if (way == 2) {
                        oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 54/100;
                        $('.distance_oil:eq('+rowIndex+')').val(oil);
                    }
                    else if (way == 3) {
                        oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 45/100;
                        $('.distance_oil:eq('+rowIndex+')').val(oil);
                    }
                    else if (way == 4) {
                        oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 48/100;
                        $('.distance_oil:eq('+rowIndex+')').val(oil);
                    }

                    var sum = 0;
                    $('.distance_km').each(function(){
                        sum += parseFloat(get_number(this));
                    });

                    $('#road_km').val(sum);
                    $("#road_km").val(function(index, value) {
                        return value
                          .replace(/[^0-9-.]/g, "")
                          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
                        ;
                      });

                    var sum_oil = 0;
                    $('.distance_oil').each(function(){
                        sum_oil += parseFloat(get_number(this));
                    });

                    $('#road_oil').val(sum_oil);
                    $("#road_oil").val(function(index, value) {
                        return value
                          .replace(/[^0-9-.]/g, "")
                          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
                        ;
                      });

                    $(".distance_km").val(function(index, value) {
                    return value
                      .replace(/[^0-9-.]/g, "")
                      .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
                    ;
                  });
               });


                $('.number').focus(function(){
                        if ( $(this).val() == '0') {
                           $(this).val(""); 
                        };
                        
                    });
                    $('.number').blur(function(){
                        if ( $(this).val() == "") {
                            $(this).val(0);
                        };
                        
                    });

                     $('.number').keyup(function(event) {

                      // skip for arrow keys
                  if(event.which >= 37 && event.which <= 40) return;

                  // format number
                  $(this).val(function(index, value) {
                    return value
                      .replace(/[^0-9-.]/g, "")
                      .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
                    ;
                  });
              });
            }
        });
        
        var road_from = $(this).find("#road_from_"+$(this).attr('id')).text();
        var road_to = $(this).find("#road_to_"+$(this).attr('id')).text();
        var road_km = $(this).find("#road_km_"+$(this).attr('id')).text();
        var road_oil = $(this).find("#road_oil_"+$(this).attr('id')).text();
        var road_time = $(this).find("#road_time_"+$(this).attr('id')).text();
        var bridge_cost = $(this).find("#bridge_cost_"+$(this).attr('id')).text();
        var police_cost = $(this).find("#police_cost_"+$(this).attr('id')).text();
        var tire_cost = $(this).find("#tire_cost_"+$(this).attr('id')).text();
        var way = $(this).find("#way_"+$(this).attr('id')).attr('data');
        var charge_add = $(this).find("#charge_add_"+$(this).attr('id')).text();
        var start_time = $(this).find("#start_time_"+$(this).attr('id')).text();
        var end_time = $(this).find("#end_time_"+$(this).attr('id')).text();
        var status = $(this).find("#status_"+$(this).attr('id')).attr('data');
        //alert(road_from);
        $('#yes').val($(this).attr('id'));
        
        $('#road_from').val(road_from);
        $('#road_to').val(road_to);
        $('#road_km').val(road_km);
        $('#road_oil').val(road_oil);
        $('#road_time').val(road_time);
        $('#bridge_cost').val(bridge_cost);
        $('#police_cost').val(police_cost);
        $('#tire_cost').val(tire_cost);
        $('#charge_add').val(charge_add);
        $('#start_time').val(start_time.replace(/\//g,"-"));
        $('#end_time').val(end_time.replace(/\//g,"-"));
        
        var radioLength = $("input[name='way']").length;
        for(var i = 0; i < radioLength; i++) {
            $("input[name='way']")[i].checked = false;
            if($("input[name='way']")[i].value == way) {
                $("input[name='way']")[i].checked = true;
            }
        }

        var radioLength2 = $("input[name='status']").length;
        for(var i = 0; i < radioLength2; i++) {
            $("input[name='status']")[i].checked = false;
            if($("input[name='status']")[i].value == status) {
                $("input[name='status']")[i].checked = true;
            }
        }

        $('#road_from').attr('data',"");
        $("#road_from").prop('readonly', true);
        $("#road_from").prop('disabled', true);
        $("#road_from").attr("code",'true');
        $('#road_to').attr('data',"");
        $("#road_to").prop('readonly', true);
        $("#road_to").prop('disabled', true);
        $("#road_to").attr("code",'true');

        if(way == 0){
            //$("#bridge_cost").prop('disabled', true);
            $("#police_cost").prop('disabled', true);
            $("#tire_cost").prop('disabled', true);
        }
        
    });


</script>
<script type="text/javascript">

function add_click(){
    $('#yes').val("");
    $('.add-field').slideDown(500);
    $("html, body").animate({ scrollTop: $('.add-field').offset().top }, 300);

        $('#road_from').focus();
        $('#road_km').val(0);
        $('#road_oil').val(0);
        $('#road_time').val(0);
        $('#bridge_cost').val(0);
        $('#police_cost').val(0);
        $('#tire_cost').val(0);
        $('#charge_add').val(0);
        $('#start_time').val("<?php echo date('d-m-Y') ?>");
        $('#end_time').val("<?php echo date('d-m-Y', strtotime('+3 years')) ?>");

        $("input[name='way'][value='0']").attr('checked',true);
        
        $('.number').focus(function(){
            if ( $(this).val() == '0') {
               $(this).val(""); 
            };
            
        });
        $('.number').blur(function(){
            if ( $(this).val() == "") {
                $(this).val(0);
            };
            
        });

        $("#road_from").prop('readonly', false);
        $("#road_from").prop('disabled', false);
        $("#road_from").attr("code",'false');
        $('#road_from').val("");
        $("#road_to").prop('readonly', false);
        $("#road_to").prop('disabled', false);
        $("#road_to").attr("code",'false');
        $('#road_to').val("");

        //$("#bridge_cost").prop('disabled', true);
        $("#police_cost").prop('disabled', true);
        $("#tire_cost").prop('disabled', true);


}


$(document).ready(function(){
            // Validate form
            $("#add_road").validate({
                errorElement: "span", // Định dạng cho thẻ HTML hiện thông báo lỗi
                rules: {
                    
                },
                submitHandler: function(form) {
                
                    if ($('#road_from').attr('code') == 'false') {
                        $('#road_from').val("");
                        $('#road_from').focus();
                        return false;
                    };
                    if ($('#road_to').attr('code') == 'false') {
                        $('#road_to').val("");
                        $('#road_to').focus();
                        return false;
                    };
                
                    var road_from = $('#road_from').attr('data');
                    var road_to = $('#road_to').attr('data');
                    var road_km = $('#road_km').attr('value');
                    var road_time = $('#road_time').attr('value');
                    var road_oil = $('#road_oil').attr('value');
                    var bridge_cost = $('#bridge_cost').attr('value');
                    var police_cost = $('#police_cost').attr('value');
                    var tire_cost = $('#tire_cost').attr('value');
                    var way = $('input[name=way]:checked').val();
                    var check = $('#road_from').attr('data');
                    var yes = $('#yes').attr('value');
                    var charge_add = $('#charge_add').attr('value');
                    var status = $('input[name=status]:checked').val();
                    var start_time = $('#start_time').attr('value');
                    var end_time = $('#end_time').attr('value');

                    var toll_booth = [];

                    var toll_booth_name = [];
                    var toll_booth_id = [];
                    var toll_booth_cost = [];
                    var toll_mst = [];
                    var toll_symbol = [];
                    var toll_type = [];
                    var check_vat = [];
                    var bridge_cost_id = [];

                    $('.toll_booth_name').each(function() { 
                        toll_booth_name.push($(this).val());
                        toll_booth_id.push($(this).attr('data'));
                    });

                    $('.toll_booth_cost').each(function() { 
                        toll_booth_cost.push($(this).val());
                        bridge_cost_id.push($(this).attr('data'));
                    });


                    $('.toll_mst').each(function() { 
                        toll_mst.push($(this).val());
                    });

                    $('.toll_symbol').each(function() { 
                        toll_symbol.push($(this).val());
                    });

                    $('.toll_type').each(function() { 
                        toll_type.push($(this).val());
                    });
                    $('.check_vat').each(function() { 
                        if($(this).is(':checked'))
                            check_vat.push($(this).val());
                        else
                            check_vat.push(0);
                    });


                    for (var i = 0; i < toll_booth_name.length; i++) {
                        toll_booth.push({'toll_booth_name':toll_booth_name[i], 'toll_booth_id':toll_booth_id[i], 'toll_booth_cost':toll_booth_cost[i], 'toll_mst':toll_mst[i], 'toll_symbol':toll_symbol[i], 'toll_type':toll_type[i], 'check_vat':check_vat[i], 'bridge_cost_id':bridge_cost_id[i]});
                    }

                    var distance = [];

                    var dkm = [];
                    var doil = [];
                    var dway = [];

                    $('.distance_km').each(function() { 
                        dkm.push($(this).val());
                    });

                    $('.distance_oil').each(function() { 
                        doil.push($(this).val());
                    });

                    $('.distance_way').each(function() { 
                        dway.push($(this).val());
                    });


                    for (var i = 0; i < way.length; i++) {
                        distance.push({'km':dkm[i], 'oil':doil[i], 'way':dway[i]});
                        
                        
                    };
                    
                    var action      = "them";
                 
                    $.ajax({
                        type: "POST", // phương thức gởi đi
                        url: "<?php echo BASE_URL ?>/newroad/add", // nơi mà dữ liệu sẽ chuyển đến khi submit
                        data: {
                            road_from: road_from,
                            road_to: road_to,
                            road_km: road_km,
                            road_time: road_time,
                            road_oil: road_oil,
                            bridge_cost: bridge_cost,
                            police_cost: police_cost,
                            tire_cost: tire_cost,
                            way: way,
                            charge_add: charge_add,
                            start_time: start_time,
                            end_time: end_time,
                            status: status,
                            check: check,
                            toll_booth: toll_booth,
                            distance: distance,
                            yes: yes,
                            }, // giá trị post
                        success: function(answer){ // if everything goes well
                            //alert(answer);
                            //console.log(answer);
                            $('#error').hide();
                            $('#error').slideToggle(100); // hiển thị thẻ div success
                            $('#error').html('<div class="successbox">'+answer+'</div>'); // đặt kết quả trả về từ test.php vào thẻ div success
                            $('#error').fadeOut(10000);

                            if (check == "") {
                                setTimeout(function() {
                                        sapxep('<?php echo $page ?>','<?php echo $order_by ?>','<?php echo $order ?>');
                                      }, 200);

                                $('#road_time_'+yes).text(road_time);
                                $('#road_km_'+yes).text(road_km);
                                $('#road_oil_'+yes).text(road_oil);
                                $('#bridge_cost_'+yes).text(bridge_cost);
                                $('#police_cost_'+yes).text(police_cost);
                                $('#tire_cost_'+yes).text(tire_cost);
                                $('#way_'+yes).text(way==1?"Lên":(way==2?"Xuống":(way==3?"Bằng":(way==4?"ĐN-QN":"Rỗng"))));
                                $('#charge_add_'+yes).text(charge_add);
                                $('#status_'+yes).text(status==1?"Sử dụng":"Không sử dụng");
                                $('#start_time_'+yes).text(start_time);
                                $('#end_time_'+yes).text(end_time);

                                
                            }
                            else{
                                if (answer.trim() != "Bảng định mức này đã tồn tại") {
                                    setTimeout(function() {
                                        sapxep('<?php echo $page ?>','<?php echo $order_by ?>','<?php echo $order ?>');
                                      }, 200);
                                    
                                    var lastID = parseInt($('#lasted').html());
                                    var rowCount = parseInt($('.table_data > tbody > tr').length);
                                    var chieu = way==1?"Lên":(way==2?"Xuống":(way==3?"Bằng":(way==4?"ĐN-QN":"Rỗng")));
                                    $('.table_data > tbody:last')
                                    .append(
                                        '<tr onclick="HighLightTR(this,"#4d90fe","cc3333");" id="'+parseInt(lastID+1)+'" class="edit_tr"><td><input name="check[]" type="checkbox" class="checkbox" value="'+parseInt(lastID+1)+'"></td><td class="fix">'+parseInt(rowCount+1)+'</td><td class="fix" id="road_from_'+parseInt(lastID+1)+'">'+$( "#road_from" ).val()+'</td><td class="fix" id="road_to_'+parseInt(lastID+1)+'">'+$( "#road_to" ).val()+'</td><td class="fix" id="way_'+parseInt(lastID+1)+'">'+chieu+'</td><td class="fix" id="road_km_'+parseInt(lastID+1)+'">'+road_km+'</td><td class="fix" id="road_oil_'+parseInt(lastID+1)+'">'+road_oil+'</td><td class="fix" id="bridge_cost_'+parseInt(lastID+1)+'">'+bridge_cost+'</td><td class="fix" id="police_cost_'+parseInt(lastID+1)+'">'+police_cost+'</td><td class="fix" id="tire_cost_'+parseInt(lastID+1)+'">'+tire_cost+'</td><td class="fix" id="charge_add_'+parseInt(lastID+1)+'">'+charge_add+'</td><td class="fix" id="road_time_'+parseInt(lastID+1)+'">'+road_time+'</td><td class="fix" id="start_time_'+parseInt(lastID+1)+'">'+start_time+'</td><td class="fix" id="end_time_'+parseInt(lastID+1)+'">'+end_time+'</td><td class="fix" id="status_'+parseInt(lastID+1)+'">'+(status==1?"Sử dụng":"Không sử dụng")+'</td><td><a class="error" href="#">Sửa</a> | <a class="error" onclick="del('+parseInt(lastID+1)+')">Xóa</a></td></tr>'
                                    );

                                    $('#lasted').html(parseInt(lastID+2));
                                }
                            }
                        }
                    });
                    return false;
                     
                 }
            });
   

   $('input[type=radio][name=way]').change(function() {
    var oil = 0;
        if (this.value == 0) {
            //$("#bridge_cost").prop('disabled', true);
            //$('#bridge_cost').val(0);
            $("#police_cost").prop('disabled', true);
            $('#police_cost').val(0);
            $("#tire_cost").prop('disabled', true);
            $('#tire_cost').val(0);

            
            oil = parseInt(get_number('#road_km')) * 38/100;
            $('#road_oil').val(oil);
        }
        else{
            //$("#bridge_cost").prop('disabled', false);
            $("#police_cost").prop('disabled', false);
            $("#tire_cost").prop('disabled', false);

            if (this.value == 1) {
                oil = parseInt(get_number('#road_km')) * 58/100;
                $('#road_oil').val(oil);
            }
            else if (this.value == 2) {
                oil = parseInt(get_number('#road_km')) * 54/100;
                $('#road_oil').val(oil);
            }
            else if (this.value == 3) {
                oil = parseInt(get_number('#road_km')) * 45/100;
                $('#road_oil').val(oil);
            }
            else if (this.value == 4) {
                oil = parseInt(get_number('#road_km')) * 48/100;
                $('#road_oil').val(oil);
            }
        }
    });

   $('#road_km').keyup(function(){
        var way = $('input[name=way]:checked').val();
        var oil = 0;
        if (way == 0) {
            oil = parseInt(get_number('#road_km')) * 38/100;
            $('#road_oil').val(oil);
        }
        else if (way == 1) {
            oil = parseInt(get_number('#road_km')) * 58/100;
            $('#road_oil').val(oil);
        }
        else if (way == 2) {
            oil = parseInt(get_number('#road_km')) * 54/100;
            $('#road_oil').val(oil);
        }
        else if (way == 3) {
            oil = parseInt(get_number('#road_km')) * 45/100;
            $('#road_oil').val(oil);
        }
        else if (way == 4) {
            oil = parseInt(get_number('#road_km')) * 48/100;
            $('#road_oil').val(oil);
        }
   });

   $('#road_from').keyup(function(){
      
        var keyword = $(this).val();
        $.ajax({
            url: '<?php echo BASE_URL ?>/road/getroadfrom',
            type: 'POST',
            data: {keyword:keyword},
            success:function(data){
                $('#road_from_list_id').slideDown(200);
                $('#road_from_list_id').html(data);
            }
        });
        if ($('#road_from').val() == "" || $('#road_from').attr('data') == "") {
            //$('#loc_from').val("");
            $('#road_from').attr('data',"");
            $('#road_from').attr('code',false);
        }
      
   });
   $('#road_to').keyup(function(){
      
        var keyword = $(this).val();
        $.ajax({
            url: '<?php echo BASE_URL ?>/road/getroadto',
            type: 'POST',
            data: {keyword:keyword},
            success:function(data){
                $('#road_to_list_id').slideDown(200);
                $('#road_to_list_id').html(data);
            }
        });
        if ($('#road_to').val() == "" || $('#road_to').attr('data') == "") {
            //$('#loc_from').val("");
            $('#road_to').attr('data',"");
            $('#road_to').attr('code',false);
        }
      
   });

   $('.toll_booth_name').keyup(function(){
        var row = $(this).parent().parent().parent().parent().parent().parent();
        var rowIndex = row[0].rowIndex;

        var keyword = $(this).val();
        $.ajax({
            url: '<?php echo BASE_URL ?>/road/gettollbooth',
            type: 'POST',
            data: {keyword:keyword, offset:rowIndex},
            success:function(data){
                $('.name_list_id:eq('+rowIndex+')').slideDown(200);
                $('.name_list_id:eq('+rowIndex+')').html(data);
            }
        });
        if ($('.toll_booth_name:eq('+rowIndex+')').val() == "" || $('.toll_booth_name:eq('+rowIndex+')').attr('data') == "") {
            
            $('.toll_booth_name:eq('+rowIndex+')').attr('data',"");
        }

        
    });

$('.toll_booth_name').on('keydown', function() {
        var row = $(this).parent().parent().parent().parent().parent().parent();
        var rowIndex = row[0].rowIndex;

        var key = event.keyCode || event.charCode;

        if( key == 8 || key == 46 ){
            $('.toll_booth_name:eq('+rowIndex+')').attr('data',"");
        }
            
      });  

$('.toll_booth_cost').keyup(function(){
    var sum = 0;
    $(".toll_booth_cost").each(function(){
        sum += +$(this).val().replace(/[^0-9\.-]+/g,"");
    });
    $("#bridge_cost").val(sum);

    $("#bridge_cost").val(function(index, value) {
        return value
          .replace(/[^0-9-.]/g, "")
          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
        ;
      });
});

   
   $('.number').keyup(function(event) {

          // skip for arrow keys
      if(event.which >= 37 && event.which <= 40) return;

      // format number
      $(this).val(function(index, value) {
        return value
          .replace(/[^0-9-.]/g, "")
          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
        ;
      });
    });
    

});

function set_item_other(item,value,mst,type,symbol,vitri) {
    // change input value
    $('.toll_booth_name:eq('+vitri+')').val(value);
    $('.toll_booth_name:eq('+vitri+')').attr("data",item);
    $('.toll_mst:eq('+vitri+')').val(mst);
    $('.toll_symbol:eq('+vitri+')').val(symbol);
    $('.toll_symbol:eq('+vitri+') option[value='+type+']').attr('selected','selected');
    // hide proposition list
    $('.name_list_id').hide();
    $('.toll_booth_name:eq('+vitri+')').focus();

}
function get_number(id){
    return $(id).val().replace(/\,/g,'');
}
function set_item_road_from(value,name) {
    // change input value
    $('#road_from').val(name);
    $("#road_from").attr("data",value);
    $("#road_from").attr("code",'true');
    
    // hide proposition list
    $('#road_from_list_id').hide();
    $('#road_from').focus();

    

}
function set_item_road_to(value,name) {
    // change input value
    $('#road_to').val(name);
    $("#road_to").attr("data",value);
    $("#road_to").attr("code",'true');
    
    // hide proposition list
    $('#road_to_list_id').hide();
    $('#road_to').focus();
     
     
}
</script>
<div style="display:none" id="lasted"></div>
<script type="text/javascript">
    var count = parseInt(<?php echo $lastID ?>);
    $('#lasted').html(count);

    $("#import_excel").click(function(){
        $("#winpopup").dialog({
            draggable:true,
            modal: true,
            autoOpen: false,
            height:200,
            width:500,
            resizable: false,
            title:'Import Excel',
            
        });
        $("#winpopup").load($(this).attr('href'));
        $("#winpopup").dialog("open");
         
        return false;
    });


var ch = "<?php echo $limit ?>";
$('#chonloc option[value='+ch+']').attr('selected','selected');
var v = "<?php echo $status ?>";
$('#sl_round option[value='+v+']').attr('selected','selected');
var di = "<?php echo $batdau ?>";
$('#batdau option[value='+di+']').attr('selected','selected');
var den = "<?php echo $ketthuc ?>";
$('#ketthuc option[value='+den+']').attr('selected','selected');

var pickerOpts = {
        closeText: "Đóng",
        currentText: "Hiện tại",
        nextText: "Tiếp",
        prevText: "Quay lại",
        monthNames: ["Tháng 1", "Tháng 2", "Tháng 3", "Tháng 4", "Tháng 5", "Tháng 6", "Tháng 7", "Tháng 8", "Tháng 9", "Tháng 10", "Tháng 11", "Tháng 12"],
        monthNamesShort: ["Tháng 1", "Tháng 2", "Tháng 3", "Tháng 4", "Tháng 5", "Tháng 6", "Tháng 7", "Tháng 8", "Tháng 9", "Tháng 10", "Tháng 11", "Tháng 12"],
        dayNames: ["Chủ nhật", "Thứ hai", "Thứ ba", "Thứ tư", "Thứ năm", "Thứ sáu", "Thứ bảy"],
        dayNamesShort: ["CN", "T2", "T3", "T4", "T5", "T6", "T7"],
        dayNamesMin: ["CN", "T2", "T3", "T4", "T5", "T6", "T7"],
        dateFormat: 'dd-mm-yy',
        changeMonth: true,
        changeYear: true,
        yearRange: "-100:+5",
        firstDay: 1,
        isRTL: false,
        showButtonPanel: true,
        onSelect: function(dateStr) {
            var d = $.datepicker.parseDate('dd-mm-yy', dateStr);
            var years = parseInt(3, 10);

            d.setFullYear(d.getFullYear() + years);

            $('#end_time').datepicker('setDate', d);
            
        },
        onClose: function(selectedDate) {
                
                $( "#end_time" ).datepicker( "option", "minDate", selectedDate );

         },
    }; 
    $("#start_time").datepicker(pickerOpts);

    var pickerOpts2 = {
        closeText: "Đóng",
        currentText: "Hiện tại",
        nextText: "Tiếp",
        prevText: "Quay lại",
        monthNames: ["Tháng 1", "Tháng 2", "Tháng 3", "Tháng 4", "Tháng 5", "Tháng 6", "Tháng 7", "Tháng 8", "Tháng 9", "Tháng 10", "Tháng 11", "Tháng 12"],
        monthNamesShort: ["Tháng 1", "Tháng 2", "Tháng 3", "Tháng 4", "Tháng 5", "Tháng 6", "Tháng 7", "Tháng 8", "Tháng 9", "Tháng 10", "Tháng 11", "Tháng 12"],
        dayNames: ["Chủ nhật", "Thứ hai", "Thứ ba", "Thứ tư", "Thứ năm", "Thứ sáu", "Thứ bảy"],
        dayNamesShort: ["CN", "T2", "T3", "T4", "T5", "T6", "T7"],
        dayNamesMin: ["CN", "T2", "T3", "T4", "T5", "T6", "T7"],
        dateFormat: 'dd-mm-yy',
        changeMonth: true,
        changeYear: true,
        yearRange: "-100:+5",
        firstDay: 1,
        isRTL: false,
        showButtonPanel: true,
        onClose: function(selectedDate) {
                
                $( "#start_time" ).datepicker( "option", "maxDate", selectedDate );

         },
    }; 
    $("#end_time").datepicker(pickerOpts2);

function addRow(tableID){
    var table=document.getElementById(tableID);
    var rowCount=table.rows.length;
    var row=table.insertRow(rowCount);
    var colCount=table.rows[0].cells.length;
    for(var i=0;i<colCount;i++){
        var newcell=row.insertCell(i);
        newcell.innerHTML=table.rows[0].cells[i].innerHTML;
        switch(newcell.childNodes[0].type){
            case"text":newcell.childNodes[0].value="";
            break;
            case"checkbox":newcell.childNodes[0].checked=false;
            break;
            case"select-one":newcell.childNodes[0].selectedIndex=0;
            break;
        }
    }

    $('.toll_booth_name:last').attr('disabled',false);
    $('.toll_booth_name:last').attr('data',"");
    $('.toll_booth_name:last').val("");
    $('.toll_booth_cost:last').val("");
    $('.toll_booth_cost:last').attr("data",0);
    
    $('input[name="chk"]:last').attr('data',"");
    $('input[name="chk"]:last').attr('tabindex',0);
    $('input[name="chk"]:last').attr('title',"");
    $('input[name="chk"]:last').attr('class',"");
    $('input[name="chk"]:last').attr('alt',0);

$('.toll_booth_cost').keyup(function(){
    var sum = 0;
    $(".toll_booth_cost").each(function(){
        sum += +$(this).val().replace(/[^0-9\.-]+/g,"");
    });
    $("#bridge_cost").val(sum);
    $("#bridge_cost").val(function(index, value) {
        return value
          .replace(/[^0-9-.]/g, "")
          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
        ;
      });
});

$('.toll_booth_name').keyup(function(){
        var row = $(this).parent().parent().parent().parent().parent().parent();
        var rowIndex = row[0].rowIndex;

        var keyword = $(this).val();
        $.ajax({
            url: '<?php echo BASE_URL ?>/road/gettollbooth',
            type: 'POST',
            data: {keyword:keyword, offset:rowIndex},
            success:function(data){
                $('.name_list_id:eq('+rowIndex+')').slideDown(200);
                $('.name_list_id:eq('+rowIndex+')').html(data);
            }
        });
        if ($('.toll_booth_name:eq('+rowIndex+')').val() == "" || $('.toll_booth_name:eq('+rowIndex+')').attr('data') == "") {
            
            $('.toll_booth_name:eq('+rowIndex+')').attr('data',"");
        }

        
    });

$('.toll_booth_name').on('keydown', function() {
        var row = $(this).parent().parent().parent().parent().parent().parent();
        var rowIndex = row[0].rowIndex;

        var key = event.keyCode || event.charCode;

        if( key == 8 || key == 46 ){
            $('.toll_booth_name:eq('+rowIndex+')').attr('data',"");
        }
            
      });  

function set_item_other(item,value,mst,type,symbol,vitri) {
    // change input value
    $('.toll_booth_name:eq('+vitri+')').val(value);
    $('.toll_booth_name:eq('+vitri+')').attr("data",item);
    $('.toll_mst:eq('+vitri+')').val(mst);
    $('.toll_symbol:eq('+vitri+')').val(symbol);
    $('.toll_symbol:eq('+vitri+') option[value='+type+']').attr('selected','selected');
    // hide proposition list
    $('.name_list_id').hide();
    $('.toll_booth_name:eq('+vitri+')').focus();

}

function get_number(id){
    return $(id).val().replace(/[^0-9\.-]+/g,"");
}

    $('.number').focus(function(){
            if ( $(this).val() == '0') {
               $(this).val(""); 
            };
            
        });
        $('.number').blur(function(){
            if ( $(this).val() == "") {
                $(this).val(0);
            };
            
        });

         $('.number').keyup(function(event) {

          // skip for arrow keys
      if(event.which >= 37 && event.which <= 40) return;

      // format number
      $(this).val(function(index, value) {
        return value
          .replace(/[^0-9-.]/g, "")
          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
        ;
      });
    });

}
function deleteRow(tableID){
    try{
        var table=document.getElementById(tableID);
        var rowCount=table.rows.length;

        var r = confirm("Bạn có chắc chắn không?");
        if (r == true){
            for(var i=0;i<rowCount;i++){
                var row=table.rows[i];
                var chkbox=row.cells[0].childNodes[0];
                if(null!=chkbox&&true==chkbox.checked){
                    if(chkbox.getAttribute("data") > 0){
                        
                            var road = chkbox.getAttribute("data");
                            var toll_booth = chkbox.getAttribute("tabindex");
                            var bridge_cost_id = chkbox.getAttribute("alt");
                            $.post("<?php echo BASE_URL ?>/road/deletetollbooth", {road: road, toll_booth: toll_booth, bridge_cost_id: bridge_cost_id},
                               function(data){
                                //alert(data);
                               }); 
                        
                    }
                    if(rowCount<=1){
                        alert("Cannot delete all the rows.");
                        break;
                    }
                    table.deleteRow(i);
                    rowCount--;
                    i--;
                }
            }
        }
    }
    catch(e){
        alert(e);
    }
}

function addRow2(tableID){
    var table=document.getElementById(tableID);
    var rowCount=table.rows.length;
    var row=table.insertRow(rowCount);
    var colCount=table.rows[0].cells.length;
    for(var i=0;i<colCount;i++){
        var newcell=row.insertCell(i);
        newcell.innerHTML=table.rows[0].cells[i].innerHTML;
        switch(newcell.childNodes[0].type){
            case"text":newcell.childNodes[0].value="";
            break;
            case"checkbox":newcell.childNodes[0].checked=false;
            break;
            case"select-one":newcell.childNodes[0].selectedIndex=0;
            break;
        }
    }

    
    $('.distance_km:last').val("");
    $('.distance_way:last').val("");
    $('.distance_oil:last').val("");
    
    $('input[name="chk2"]:last').attr('data',"");
    $('input[name="chk2"]:last').attr('tabindex',0);
    $('input[name="chk2"]:last').attr('title',"");
    $('input[name="chk2"]:last').attr('alt',"");



$('.distance_way').change(function() {
    var row = $(this).parent().parent().parent().parent().parent().parent();
    var rowIndex = row[0].rowIndex;

    var oil = 0;
        if (this.value == 0) {
                        
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 38/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }
        else{
            if (this.value == 1) {
                oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 58/100;
                $('.distance_oil:eq('+rowIndex+')').val(oil);
            }
            else if (this.value == 2) {
                oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 54/100;
                $('.distance_oil:eq('+rowIndex+')').val(oil);
            }
            else if (this.value == 3) {
                oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 45/100;
                $('.distance_oil:eq('+rowIndex+')').val(oil);
            }
            else if (this.value == 4) {
                oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 48/100;
                $('.distance_oil:eq('+rowIndex+')').val(oil);
            }
        }

        var sum = 0;
        $('.distance_km').each(function(){
            sum += parseFloat(get_number(this));
        });

        $('#road_km').val(sum);
        $("#road_km").val(function(index, value) {
            return value
              .replace(/[^0-9-.]/g, "")
              .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
            ;
          });

        var sum_oil = 0;
        $('.distance_oil').each(function(){
            sum_oil += parseFloat(get_number(this));
        });

        $('#road_oil').val(sum_oil);
        $("#road_oil").val(function(index, value) {
            return value
              .replace(/[^0-9-.]/g, "")
              .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
            ;
          });

    });

   $('.distance_km').keyup(function(){
        var row = $(this).parent().parent().parent().parent().parent().parent();
        var rowIndex = row[0].rowIndex;

        var way = $('.distance_way:eq('+rowIndex+')').val();
        var oil = 0;
        if (way == 0) {
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 38/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }
        else if (way == 1) {
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 58/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }
        else if (way == 2) {
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 54/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }
        else if (way == 3) {
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 45/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }
        else if (way == 4) {
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 48/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }

        var sum = 0;
        $('.distance_km').each(function(){
            sum += parseFloat(get_number(this));
        });

        $('#road_km').val(sum);
        $("#road_km").val(function(index, value) {
            return value
              .replace(/[^0-9-.]/g, "")
              .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
            ;
          });

        var sum_oil = 0;
        $('.distance_oil').each(function(){
            sum_oil += parseFloat(get_number(this));
        });

        $('#road_oil').val(sum_oil);
        $("#road_oil").val(function(index, value) {
            return value
              .replace(/[^0-9-.]/g, "")
              .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
            ;
          });

        $(".distance_km").val(function(index, value) {
        return value
          .replace(/[^0-9-.]/g, "")
          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
        ;
      });
   });


    $('.number').focus(function(){
            if ( $(this).val() == '0') {
               $(this).val(""); 
            };
            
        });
        $('.number').blur(function(){
            if ( $(this).val() == "") {
                $(this).val(0);
            };
            
        });

         $('.number').keyup(function(event) {

          // skip for arrow keys
      if(event.which >= 37 && event.which <= 40) return;

      // format number
      $(this).val(function(index, value) {
        return value
          .replace(/[^0-9-.]/g, "")
          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
        ;
      });
    });

}
function deleteRow2(tableID){
    try{
        var table=document.getElementById(tableID);
        var rowCount=table.rows.length;

        var r = confirm("Bạn có chắc chắn không?");
        if (r == true){
            for(var i=0;i<rowCount;i++){
                var row=table.rows[i];
                var chkbox=row.cells[0].childNodes[0];
                if(null!=chkbox&&true==chkbox.checked){
                    if(chkbox.getAttribute("title") > 0){
                        
                            var km = chkbox.getAttribute("data");
                            var road = chkbox.getAttribute("tabindex");
                            var oil = chkbox.getAttribute("title");
                            var way = chkbox.getAttribute("alt");
                            $.post("<?php echo BASE_URL ?>/road/deletedistance", {road: road, km: km, oil: oil, way: way},
                               function(data){
                                //alert(data);
                               }); 
                        
                    }
                    if(rowCount<=1){
                        alert("Cannot delete all the rows.");
                        break;
                    }
                    table.deleteRow(i);
                    rowCount--;
                    i--;
                }
            }
        }
    }
    catch(e){
        alert(e);
    }
}


$('.distance_way').change(function() {
    var row = $(this).parent().parent().parent().parent().parent().parent();
    var rowIndex = row[0].rowIndex;

    var oil = 0;
        if (this.value == 0) {
                        
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 38/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }
        else{
            if (this.value == 1) {
                oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 58/100;
                $('.distance_oil:eq('+rowIndex+')').val(oil);
            }
            else if (this.value == 2) {
                oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 54/100;
                $('.distance_oil:eq('+rowIndex+')').val(oil);
            }
            else if (this.value == 3) {
                oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 45/100;
                $('.distance_oil:eq('+rowIndex+')').val(oil);
            }
            else if (this.value == 4) {
                oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 48/100;
                $('.distance_oil:eq('+rowIndex+')').val(oil);
            }
        }

        var sum = 0;
        $('.distance_km').each(function(){
            sum += parseFloat(get_number(this));
        });

        $('#road_km').val(sum);
        $("#road_km").val(function(index, value) {
            return value
              .replace(/[^0-9-.]/g, "")
              .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
            ;
          });

        var sum_oil = 0;
        $('.distance_oil').each(function(){
            sum_oil += parseFloat(get_number(this));
        });

        $('#road_oil').val(sum_oil);
        $("#road_oil").val(function(index, value) {
            return value
              .replace(/[^0-9-.]/g, "")
              .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
            ;
          });

    });

   $('.distance_km').keyup(function(){
        var row = $(this).parent().parent().parent().parent().parent().parent();
        var rowIndex = row[0].rowIndex;

        var way = $('.distance_way:eq('+rowIndex+')').val();
        var oil = 0;
        if (way == 0) {
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 38/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }
        else if (way == 1) {
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 58/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }
        else if (way == 2) {
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 54/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }
        else if (way == 3) {
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 45/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }
        else if (way == 4) {
            oil = parseInt(get_number('.distance_km:eq('+rowIndex+')')) * 48/100;
            $('.distance_oil:eq('+rowIndex+')').val(oil);
        }

        var sum = 0;
        $('.distance_km').each(function(){
            sum += parseFloat(get_number(this));
        });

        $('#road_km').val(sum);
        $("#road_km").val(function(index, value) {
            return value
              .replace(/[^0-9-.]/g, "")
              .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
            ;
          });

        var sum_oil = 0;
        $('.distance_oil').each(function(){
            sum_oil += parseFloat(get_number(this));
        });

        $('#road_oil').val(sum_oil);
        $("#road_oil").val(function(index, value) {
            return value
              .replace(/[^0-9-.]/g, "")
              .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
            ;
          });

        $(".distance_km").val(function(index, value) {
        return value
          .replace(/[^0-9-.]/g, "")
          .replace(/\B(?=(\d{3})+(?!\d))/g, ",")
        ;
      });
   });

</script>
</div>
<style type="text/css">
    #ui-datepicker-div{
        font-size: 13px;
    }
</style>